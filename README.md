So in prisma related errors

Prisma looks for the default location to load the configuation and the schema, if the directory is custom then need to update the path of the schema, 
apart from that dotenv have the config function instead of the require function.